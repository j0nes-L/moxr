<script>
    const path = window.location.pathname.toLowerCase();
    const prefix = import.meta.env.PROD ? '/moxr' : '';
    if (!path.startsWith(`${prefix}/de/`) && !path.startsWith(`${prefix}/en/`)) {
        window.location.href = `${prefix}/de/`;
    }
</script>
